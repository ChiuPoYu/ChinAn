You are designing a data table UI for an enterprise Vue SPA.

Tech stack:
- Vue 3
- Element Plus
- Backend uses ListResponse<T> with page, pageSize, total

Task:
Design a table UI for listing: [ENTITY_NAME]

Requirements:
- Include column headers for key fields
- Include action column (view / edit / delete)
- Pagination must be placed at the bottom
- Assume server-side pagination
- Include empty state and loading state placeholders

Constraints:
- No client-side filtering logic
- No API calls
- Use Element Plus table and pagination components only

Output:
- Enterprise-style table layout suitable for integration with Vue Query.